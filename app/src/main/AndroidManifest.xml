<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pharmacy.android.dwa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Home.FirstAcivityLanguage"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Home.MainHome"
            android:theme="@style/MainHome" />
        <activity
            android:name=".LogReg.Login"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".LogReg.Regisration"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".Cart.MainCart"
            android:theme="@style/MainHome" />
        <activity
            android:name=".LogReg.ChooseUser"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".Drivers.MainDrivers"
            android:theme="@style/MainHome" />
    </application>

</manifest>